import { useLoaderData } from "react-router-dom";


const WatchList = () => {

    const data = useLoaderData();
    console.log(data);

    return (
        <div>
           
        </div>
    );
};

export default WatchList;